const { client } = require('../middleware/connectionMW');

class Postponed {
	constructor() {}
	//get pins which should be posted this minute
	// async getPins() {
	// 	const pinsCollection = await client.db().collection('pins');
	// 	pinsCollection.aggregate([
	// 		$match:{

	// 		}
	// 	])
	// }
}
